{
  "name": "app",
  "version": "1.0.0",
  "description": "Express app integrated with Jenkins",
  "main": "index.js",
  "type": "commonjs",
  "scripts": {
    "start": "node index.js",
    "build": "echo 'Standardizing structure for Jenkins...' && mkdir -p dist && cp index.js dist/ && cp package.json dist/",
    "test": "jest ./tests/unit",
    "lint": "eslint .",
    "integration-tests": "jest ./tests/integration"
  },
  "dependencies": {
    "express": "^5.2.1"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "jest": "^29.7.0",
    "supertest": "^6.3.4"
  }
}